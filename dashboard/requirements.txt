# Local development, Streamlit dashboard, scheduler, and optional Trends support.
# Kept below dashboard/ so Vercel does not install these large UI dependencies
# into the FastAPI serverless function.
-r ../requirements.txt

uvicorn[standard]==0.30.6
APScheduler==3.10.4
streamlit==1.38.0
plotly==5.24.1
pandas==2.2.3
pytrends==4.9.2

