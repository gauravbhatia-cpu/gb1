# Local development, Streamlit dashboard, scheduler, and optional Trends support.
# This deliberately is not named requirements.txt so Vercel's Python runtime
# does not discover it as a production dependency manifest.
-r ../requirements.txt

uvicorn[standard]==0.30.6
APScheduler==3.10.4
streamlit==1.38.0
plotly==5.24.1
pandas==2.2.3
pytrends==4.9.2

